if [ ! -f /etc/hosts.allow ]
then
    /usr/bin/mkdir -p /etc
    /usr/bin/cp /etc/defaults/etc/hosts.allow /etc/hosts.allow
fi

if [ ! -f /etc/hosts.deny ]
then
    /usr/bin/mkdir -p /etc
    /usr/bin/cp /etc/defaults/etc/hosts.deny /etc/hosts.deny
fi

